import React from 'react'

const aSquire = (props) => {
    return(
        <div className='squire' style={{backgroundColor: `${props.color}`}}>
        
        </div>
    )
}

export default aSquire